# ==================== 开发环境变量 ====================
# 仅在 `vite dev` 模式下加载
# 变量名必须以 VITE_ 开头才能在客户端代码中通过 import.meta.env 访问

# 后端 API 基础地址（替换为你的开发环境地址）
VITE_API_BASE_URL=http://localhost:8080

# 是否启用 sourcemap（true = 方便调试，但构建略慢）
VITE_SOURCEMAP=true
